// Copia este archivo como environment.ts y environment.prod.ts y completa los valores.
// Estos archivos están en .gitignore — nunca subas credenciales al repositorio.
export const environment = {
  production: false,          // true en environment.prod.ts
  apiUrl: 'http://localhost:8080/api',
};
